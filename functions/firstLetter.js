const capitalizeFirstLetter = (str) => {
	// converting first letter to uppercase
	const capitalized = String(str).charAt(0).toUpperCase;

	return capitalized;
};

module.exports = capitalizeFirstLetter;
